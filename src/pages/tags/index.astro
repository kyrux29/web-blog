---
import { getTagCounts } from "../../lib/content";
import { withBase } from "../../utils";
import BaseLayout from "../../layouts/BaseLayout.astro";

const tags = await getTagCounts();
---

<BaseLayout title="Tags | Kyrux">
  <section class="reveal mb-6">
    <h1 class="text-4xl font-semibold tracking-tight text-neutral-100">Tags</h1>
    <p class="mt-3 max-w-3xl text-neutral-400">
      Browse techniques, platforms, and topics across CTF write-ups and research notes.
    </p>
  </section>

  <div class="reveal panel p-5">
    <div class="flex flex-wrap gap-2">
      {tags.map(({ tag, count }) => (
        <a href={withBase(`/tags/${encodeURIComponent(tag)}/`)} class="chip hover:text-cyan-300">
          {tag} <span class="ml-2 text-[10px] text-neutral-500">({count})</span>
        </a>
      ))}
    </div>
  </div>
</BaseLayout>
